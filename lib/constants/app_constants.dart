/// Global app-wide constant values (non-config). Grows as features are added.
class AppConstants {
  AppConstants._();

  static const Duration splashDuration = Duration(seconds: 2);
  static const Duration defaultAnimation = Duration(milliseconds: 250);
}
