# وثيقة تصميم نظام كشف تزوير الحضور ومشاركة الحسابات (Device Integrity & Sharing Detection)

توضح هذه الوثيقة الحل التقني لمكافحة ظاهرة "مشاركة الحسابات" بين الموظفين (مثال: قيام موظف بتسجيل الحضور لزميله المتغيب من هاتف آخر). يعتمد الحل على **ربط الحساب بـمعرّف فريد للجهاز (Hardware Device ID)**، وإرسال هذا المعرّف مع كل عملية تحضير لتمكين الإدارة من كشف المخالفين ورصد الأجهزة المشتركة دون حظر العملية فورياً (لإتاحة الفرصة للإدارة لمراقبة وضبط الموظفين المستهترين).

---

## 1. التحدي التقني: معرفات الأجهزة في الهواتف الحديثة

في أنظمة التشغيل الحديثة (Android 10+ و iOS)، تفرض الشركات (Google و Apple) قيوداً صارمة على الخصوصية لحظر الوصول إلى المعرّفات العتادية الثابتة مثل الـ IMEI أو العنوان الفيزيائي للشبكة (MAC Address).

### الحل البديل والموثوق (The Reliable Solution):
بدلاً من المعرفات المحظورة، نستخدم معرّفات برمجية فريدة وثابتة توفرها الأنظمة وتلبي متطلبات المتجرين:
1. **في نظام أندرويد (Android)**: نستخدم `Android ID` (وهو معرّف فريد من نوعه يختلف لكل تطبيق وموقع توقيع مفتاح التطبيق، ولكنه ثابت للجهاز طالما لم يتم عمل فورمات للهاتف).
2. **في نظام آي أو إس (iOS)**: نستخدم `Identifier for Vendor (IDFV)` (وهو معرّف فريد للشركة المطورة يظل ثابتاً للجهاز طالما أن هناك تطبيقاً واحداً على الأقل للمطور مثبتاً على الهاتف).
3. **المعرّف الآمن والمستمر (Persistent UUID via Secure Storage)**:
   * عند تشغيل التطبيق لأول مرة، نتحقق من وجود معرّف فريد مخزن في الذاكرة الآمنة للجهاز (Keychain لـ iOS و Keystore لـ Android).
   * إذا لم يكن موجوداً، نولّد معرّفاً فريداً عشوائياً (UUID v4) ونقوم بحفظه في الذاكرة الآمنة باستخدام حزمة `flutter_secure_storage`.
   * يتميز هذا المعرّف بأنه **لا يُمسح** حتى لو قام المستخدم بمسح بيانات التطبيق أو إلغاء تثبيته وإعادة تنزيله مجدداً، مما يجعله بمثابة البصمة الرقمية الثابتة للهاتف.

---

## 2. آلية العمل البرمجية وسير البيانات (Data & Logging Flow)

```
[هاتف الموظف] --(يرسل بصمة الجهاز الدورية + البيانات)--> [السيرفر API] 
                                                                 |
   +-------------------------------------------------------------+
   | (مقارنة البصمة الحالية بالبصمة المسجلة للموظف)
   |
   +---> [حالة 1: البصمة مطابقة] ---> تسجل العملية كـ (حضور عادي)
   |
   +---> [حالة 2: البصمة مختلفة] ---> تسجل العملية كـ (مخالفة: جهاز غير معتمد) دون حظر الموظف
   |
   +---> [حالة 3: البصمة مكررة]  ---> تسجل العملية كـ (مخالفة: جهاز مشترك لعدة موظفين)
```

### خطوات التنفيذ (System Logic Steps):
1. **تسجيل بصمة الجهاز الأولية (Initial Device Binding)**:
   - عند أول عملية تسجيل دخول (Login) أو أول عملية تحضير ناجحة للموظف، يقوم السيرفر بربط بصمة جهازه الحالية (`deviceUuid`) بحسابه الشخصي في قاعدة البيانات وتعتبر هي **"الجهاز المعتمد الأساسي" (Primary Registered Device)**.
2. **فحص العمليات اللاحقة (Audit on Check-In)**:
   - مع كل عملية تحضير (حضور أو انصراف)، يرسل التطبيق البصمة الخاصة بالهاتف الحالي للسيرفر.
   - يقوم السيرفر بمقارنة البصمة الواردة بالبصمة المسجلة مسبقاً للموظف:
     - **إذا كانت مطابقة**: يمر التحضير بشكل طبيعي.
     - **إذا كانت مختلفة (Device Mismatch)**: يُسجل التحضير بنجاح ولكن يتم رفع علم تحذيري بالخلفية: `DEVICE_MISMATCH = 1` وتخزين معرّف الجهاز الجديد في حقل `AUDIT_DEVICE_ID`.
     - **إذا كانت البصمة مكررة مع موظف آخر (Shared Device)**: يُرفع علم تحذيري: `SHARED_DEVICE_ALERT = 1` لأن هذا الهاتف تم استخدامه لتحضير أكثر من موظف مختلف في نفس اليوم.

---

## 3. مواصفات وتعديلات الـ API

لتطبيق هذا النظام، نحتاج لإرسال تفاصيل الجهاز مع طلب التحضير `/Users/CheckInAndOut`.

### تعديل طلب التحضير (API Request Upgrade)
* **Endpoint**: `POST {{base_url}}/Users/CheckInAndOut`
* **Request Body (New Fields Added)**:
  ```json
  {
    "siteId": 51,
    "checkType": "IN",
    "attendanceDate": "17/06/2026",
    "actualTime": "08:30",
    "latitude": "30.700000",
    "longitude": "31.500000",
    "isFake": 0,
    "fakeLatitude": null,
    "fakeLongitude": null,
    "notes": "",
    
    // الحقول الجديدة الخاصة ببصمة ونوع الجهاز
    "deviceUuid": "f81d4fae-7dec-11d0-a765-00a0c91e6bf6", // بصمة الجهاز الفريدة الثابتة
    "deviceName": "Samsung Galaxy S23 Ultra", // اسم الجهاز التسويقي
    "deviceOs": "Android 14", // نظام التشغيل والنسخة
    "appVersion": "1.2.4" // نسخة التطبيق الحالية
  }
  ```

* **ملاحظة الأمان**: يتم توليد وحفظ الـ `deviceUuid` محلياً عبر الكود البرمجي للتطبيق وحفظه في الـ `SecureStorage` لضمان عدم تلاعبه أو تغييره بواسطة الموظف.

---

## 4. نموذج قاعدة البيانات المعدل (Database Schema Adjustments)

نوصي بإضافة جدول لمراقبة الأجهزة التاريخية للموظفين، بالإضافة إلى حقول فحص في سجلات الحضور:

### 1. جدول أجهزة الموظفين المعتمدة (`HR_EMPLOYEE_DEVICES`)
لتتبع الأجهزة التي يمتلكها كل موظف والأجهزة المعتمدة له:
* `ID` (PRIMARY KEY)
* `EMPLOYEE_ID` (FOREIGN KEY -> HR_EMPLOYEES)
* `DEVICE_UUID` (VARCHAR - بصمة الجهاز)
* `DEVICE_NAME` (VARCHAR)
* `DEVICE_OS` (VARCHAR)
* `STATUS` (VARCHAR: 'APPROVED' للمعتمد، 'BLOCKED' للمحظور)
* `CREATED_AT` (TIMESTAMP)

### 2. تعديل جدول الحضور والغياب المباشر (`HR_ATTENDANCE_LOG`)
إضافة حقول للتدقيق المباشر:
* `SUBMITTED_DEVICE_UUID` (VARCHAR): بصمة الجهاز الذي أرسل الطلب حالياً.
* `DEVICE_NAME` (VARCHAR): اسم الجهاز المستخدم للتحضير الحالي.
* `IS_SUSPICIOUS` (NUMBER/BOOLEAN): يوضع فيه `1` إذا تم التحضير من جهاز غير معتمد للموظف.
* `IS_SHARED` (NUMBER/BOOLEAN): يوضع فيه `1` إذا ثبت استخدام نفس البصمة لتحضير موظف آخر في نفس اليوم.

---

## 5. عرض لوحة تحكم المسؤول (Admin Dashboard Alerts)

بناءً على البيانات المجمعة، يستطيع الأدمن مشاهدة التحذيرات التالية في لوحة التحكم دون علم الموظف المخالف:

1. **فلتر "الحضور المشبوه" (Suspicious Attendance Filter)**:
   - يتيح للأدمن استعراض جميع حركات التحضير التي تمت من أجهزة غير مطابقة للجهاز الأساسي للموظف. تظهر بلون أصفر تحذيري مع بيانات الجهاز البديل (نوعه وموديله).
2. **تحذير الأجهزة المشتركة (Shared Devices Alarm)**:
   - إذا قام موظف (أ) بتحضير زميله (ب) من نفس هاتفه، يظهر تنبيه فوري للأدمن:
     > ⚠️ **تنبيه بصمة مشتركة**: تم تحضير الموظف (أ) والموظف (ب) من نفس جهاز الـ Samsung Galaxy S23 Ultra (بصمة: `f81d4f...`) في نفس اليوم بفارق زمن دقائق.
3. **تقرير تاريخ تبديل الأجهزة (Device Change History)**:
   - يوضح التقرير قائمة الأجهزة المختلفة التي استخدمها الموظف خلال الشهر ومعدل تكرار كل جهاز، مما يسهل رصد حالات التحضير الجماعية بالنيابة.
